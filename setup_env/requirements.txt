prefect==2.19.7
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-dotenv==1.0.1
awscli==1.33.13
boto3==1.34.131

# Prefect 2.x imports `from griffe.dataclasses import Docstring`, a module
# path that griffe 1.0.0 removed/reorganized (breaking change, not a real
# SemVer minor bump). Pin below 1.0.0 to avoid
# "ModuleNotFoundError: No module named 'griffe.dataclasses'".
griffe<1.0.0